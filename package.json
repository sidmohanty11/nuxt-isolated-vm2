{
  "name": "nuxt-app",
  "engines": {
    "node": "20.x"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build && cd .vercel/output/functions && prebuild-install || (node-gyp rebuild --release -j max && node-gyp clean)",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@builder.io/sdk-vue": "2.0.17",
    "nuxt": "^3.13.0",
    "vue": "latest",
    "vue-router": "latest"
  }
}
